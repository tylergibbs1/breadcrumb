{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'HOOK RUNNING'; jq -r '.tool_input.file_path // empty' | { read fp; echo \"FILE: $fp\"; [ -n \"$fp\" ] && [ -f .breadcrumbs.json ] && breadcrumb check \"$fp\" 2>/dev/null | jq -r 'if .breadcrumbs and (.breadcrumbs | length) > 0 then \"📝 NOTE: \" + (.breadcrumbs | map(.message) | join(\"; \")) else empty end'; exit 0; }"
          }
        ]
      }
    ]
  }
}
